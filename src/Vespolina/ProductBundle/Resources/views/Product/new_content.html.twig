{% form_theme form form.getOption('theme') %}
<form action="{{ path('vespolina_product_product_create') }}" {{ form_enctype(form) }} method="POST" class="vespolina_product_product_new">
    {{ form_field(form) }}
    <div>
        <input type="submit" value="{% trans 'Create Product' from 'VespolinaProductBundle' %}" />
    </div>
</form>
